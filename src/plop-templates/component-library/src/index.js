import React from "react";

const {{pascalCase name}} = ({
  children,
  displayStyles,
  displayContent,
  ...extraProps
}) => {

  return (
    <h1>{{pascalCase name}}</h1>
  );
};

export default {{pascalCase name}};
