{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "main": "build/index.js",
  "keywords": [
    "react",
    "{{name}}"
  ],
  "scripts": {
    "cleanup": "rimraf dist",
    "build": "run-s cleanup build-rollup",
    "watch": "run-p watch-rollup test-harness",
    "test-harness": "parcel tester/index.html",
    "watch-rollup": "rollup -c -w",
    "build-rollup": "rollup -c"
  },
  "repository": "",
  "author": "{{author}}",
  "license": "MIT",
  "private": false,
  "devDependencies": {

  }
}
